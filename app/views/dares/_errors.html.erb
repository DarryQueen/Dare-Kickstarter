<% if @dare.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@dare.errors.count, "error") %> prohibited
      this dare from being saved:
    </h2>
    <ul>
      <% @dare.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>
